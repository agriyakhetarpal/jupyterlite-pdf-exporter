Rich outputs This test notebook has outputs as saved by pandas, xarray, folium, Plotly, and ipywidgets, so that we can check what the exporter does with HTML and with outputs that only work in a browser. The HTML was trimmed by hand to keep the file small, but the structure is what each library writes. In [1]: import pandas as pd df = pd.DataFrame({'city': ['Paris', 'Delhi'], 'temp': [20.6, 19.3]}) df Out[1]: city temp 0 Paris 20.6 1 Delhi 19.3 In [2]: import numpy as np import xarray as xr da = xr.DataArray(np.zeros(3), dims='lat') da Out[2]: <xarray.DataArray (lat: 3)> Size: 24B In [3]: import folium m = folium.Map(location=[48.85, 2.35], zoom_start=12) m Out[3]: <folium.folium.Map at 0x1> In [4]: import plotly.express as px fig = px.scatter(df, x='city', y='temp') fig [Output not available in PDF] In [5]: import ipywidgets as widgets widgets.IntSlider(value=42) IntSlider(value=42) 1